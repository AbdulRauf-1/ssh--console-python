# Runtime dependencies for SSH Console (Python).
# Install with:  pip install -r requirements.txt
# Then run with: python -m ssh_console
fastapi>=0.110
uvicorn[standard]>=0.27
asyncssh>=2.14
cryptography>=42.0
jinja2>=3.1
python-multipart>=0.0.9
